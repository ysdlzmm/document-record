# TypeScript 类型系统

## 一、类型基础

### 1.1 基本类型

```typescript
// 基础类型注解
let name: string = '张三';
let age: number = 25;
let isActive: boolean = true;
let nothing: null = null;
let notDefined: undefined = undefined;

// 数组类型
let numbers: number[] = [1, 2, 3];
let strings: Array<string> = ['a', 'b', 'c'];

// 对象类型
let user: { name: string; age: number } = {
  name: '李四',
  age: 30
};

// 可选属性
interface User {
  name: string;
  age?: number; // 可选
  email?: string;
}

// 只读属性
interface Config {
  readonly apiKey: string;
  timeout: number;
}

const config: Config = {
  apiKey: 'xxx',
  timeout: 5000
};
// config.apiKey = 'yyy'; // ❌ 错误：只读属性不能修改
```

### 1.2 联合类型与交叉类型

```typescript
// 联合类型（Union Types）
type ID = number | string;

function printId(id: ID) {
  console.log(`ID: ${id}`);
}

printId(123);    // ✅
printId('abc');  // ✅

// 类型收窄（Type Narrowing）
function process(value: string | number) {
  if (typeof value === 'string') {
    console.log(value.toUpperCase()); // TypeScript 知道这里是 string
  } else {
    console.log(value.toFixed(2));    // 这里是 number
  }
}

// 交叉类型（Intersection Types）
interface Person {
  name: string;
}

interface Employee {
  employeeId: number;
}

type PersonEmployee = Person & Employee;

const worker: PersonEmployee = {
  name: '张三',
  employeeId: 12345
};
```

### 1.3 字面量类型

```typescript
// 字符串字面量
type Direction = 'up' | 'down' | 'left' | 'right';

function move(direction: Direction) {
  console.log(`Moving ${direction}`);
}

move('up');    // ✅
// move('diagonal'); // ❌ 错误

// 数字字面量
type DiceRoll = 1 | 2 | 3 | 4 | 5 | 6;

function rollDice(): DiceRoll {
  return Math.floor(Math.random() * 6) + 1 as DiceRoll;
}

// 布尔字面量
type Success = true;
type Failure = false;

type Result = Success | Failure;

// 结合枚举模式
type Theme = 'light' | 'dark' | 'auto';

function setTheme(theme: Theme) {
  document.documentElement.setAttribute('data-theme', theme);
}
```

---

## 二、高级类型

### 2.1 类型断言

```typescript
// 尖括号语法（JSX 中不可用）
let value: any = 'Hello';
let strLength: number = (<string>value).length;

// as 语法（推荐）
let strLength2: number = (value as string).length;

// 非空断言（!）
function greet(name: string | null) {
  // name!.toUpperCase(); // ✅ 告诉 TS name 不会是 null
}

// 双重断言（慎用）
interface User {
  name: string;
}

let user = {} as User;
user.name = '张三';
```

### 2.2 类型守卫

```typescript
// typeof 类型守卫
function process(value: string | number) {
  if (typeof value === 'string') {
    return value.toUpperCase();
  }
  return value * 2;
}

// instanceof 类型守卫
class Dog {
  bark() { console.log('Woof!'); }
}

class Cat {
  meow() { console.log('Meow!'); }
}

function speak(animal: Dog | Cat) {
  if (animal instanceof Dog) {
    animal.bark();
  } else {
    animal.meow();
  }
}

// in 操作符类型守卫
interface Bird {
  fly: boolean;
  layEggs: boolean;
}

interface Fish {
  swim: boolean;
  layEggs: boolean;
}

function move(pet: Bird | Fish) {
  if ('fly' in pet) {
    console.log('Bird');
  } else {
    console.log('Fish');
  }
}

// 自定义类型守卫
function isString(value: unknown): value is string {
  return typeof value === 'string';
}

function processUnknown(value: unknown) {
  if (isString(value)) {
    console.log(value.toUpperCase());
  }
}
```

### 2.3 判别联合类型

```typescript
// 使用判别属性区分类型
interface SuccessResponse {
  status: 'success';
  data: { id: number; name: string };
}

interface ErrorResponse {
  status: 'error';
  error: { message: string; code: number };
}

type ApiResponse = SuccessResponse | ErrorResponse;

function handleResponse(response: ApiResponse) {
  if (response.status === 'success') {
    console.log(response.data.name); // ✅ TypeScript 知道这是 SuccessResponse
  } else {
    console.log(response.error.message); // ✅ 这是 ErrorResponse
  }
}

// 完整的判别联合示例
interface LoadingState {
  type: 'loading';
}

interface SuccessState {
  type: 'success';
  data: any;
}

interface ErrorState {
  type: 'error';
  error: Error;
}

type AsyncState = LoadingState | SuccessState | ErrorState;

function renderState(state: AsyncState) {
  switch (state.type) {
    case 'loading':
      return 'Loading...';
    case 'success':
      return `Data: ${JSON.stringify(state.data)}`;
    case 'error':
      return `Error: ${state.error.message}`;
  }
}
```

---

## 三、泛型

### 3.1 泛型基础

```typescript
// 泛型函数
function identity<T>(arg: T): T {
  return arg;
}

const num = identity<number>(123);
const str = identity('hello'); // 类型推断

// 多个泛型参数
function pair<T, U>(first: T, second: U): [T, U] {
  return [first, second];
}

const [first, second] = pair('张三', 25);

// 泛型接口
interface Box<T> {
  value: T;
  getValue(): T;
}

class StringBox implements Box<string> {
  constructor(public value: string) {}

  getValue(): string {
    return this.value;
  }
}

// 泛型类
class Stack<T> {
  private items: T[] = [];

  push(item: T): void {
    this.items.push(item);
  }

  pop(): T | undefined {
    return this.items.pop();
  }

  peek(): T | undefined {
    return this.items[this.items.length - 1];
  }
}

const numberStack = new Stack<number>();
numberStack.push(1);
numberStack.push(2);
```

### 3.2 泛型约束

```typescript
// 约束泛型必须有特定属性
interface Lengthwise {
  length: number;
}

function logLength<T extends Lengthwise>(arg: T): void {
  console.log(arg.length);
}

logLength('hello');     // ✅ 字符串有 length
logLength([1, 2, 3]);   // ✅ 数组有 length
// logLength(123);      // ❌ 数字没有 length

// 约束为对象类型
function getProperty<T, K extends keyof T>(obj: T, key: K): T[K] {
  return obj[key];
}

const user = { name: '张三', age: 25 };
const name = getProperty(user, 'name');   // ✅
// const invalid = getProperty(user, 'email'); // ❌

// 多重约束
interface WithId {
  id: number;
}

interface WithTimestamp {
  createdAt: Date;
}

function createEntity<T extends WithId & WithTimestamp>(entity: T): T {
  return entity;
}

const validEntity = createEntity({
  id: 1,
  createdAt: new Date()
});
```

### 3.3 条件类型

```typescript
// 基础条件类型
type IsArray<T> = T extends any[] ? true : false;

type Test1 = IsArray<number>;    // false
type Test2 = IsArray<string[]>;  // true

// 分布式条件类型
type ToArray<T> = T extends any ? T[] : never;

type Result = ToArray<string | number>; // string[] | number[]

// 类型推断（infer）
type Unpacked<T> = T extends (infer U)[] ? U :
  T extends (...args: any[]) => infer U ? U :
  T extends Promise<infer U> ? U :
  T;

type T0 = Unpacked<string>;              // string
type T1 = Unpacked<string[]>;            // string
type T2 = Unpacked<() => string>;        // string
type T3 = Unpacked<Promise<string>>;     // string

// 实用条件类型
type NonNullable<T> = T extends null | undefined ? never : T;

type MappedType<T> = {
  [P in keyof T]?: T[P];
};

type Partial<T> = {
  [P in keyof T]?: T[P];
};

type Required<T> = {
  [P in keyof T]-?: T[P];
};
```

---

## 四、工具类型

### 4.1 常用工具类型

```typescript
// Partial - 所有属性变为可选
interface User {
  id: number;
  name: string;
  email: string;
}

type PartialUser = Partial<User>;
// { id?: number; name?: string; email?: string; }

// Required - 所有属性变为必需
type RequiredUser = Required<PartialUser>;
// { id: number; name: string; email: string; }

// Readonly - 所有属性变为只读
type ReadonlyUser = Readonly<User>;
// { readonly id: number; readonly name: string; readonly email: string; }

// Pick - 选取部分属性
type UserPreview = Pick<User, 'id' | 'name'>;
// { id: number; name: string; }

// Omit - 排除部分属性
type CreateUserDto = Omit<User, 'id'>;
// { name: string; email: string; }

// Record - 构造对象类型
type PageInfo = {
  title: string;
};

type Page = 'home' | 'about' | 'contact';

type Nav = Record<Page, PageInfo>;
// {
//   home: { title: string; };
//   about: { title: string; };
//   contact: { title: string; };
// }

// Exclude - 排除类型
type T0 = Exclude<'a' | 'b' | 'c', 'a'>;  // 'b' | 'c'

// Extract - 提取类型
type T1 = Extract<'a' | 'b' | 'c', 'a' | 'f'>;  // 'a'

// ReturnType - 获取函数返回值类型
function fn() {
  return { x: 10, y: 20 };
}

type R = ReturnType<typeof fn>;  // { x: number; y: number; }

// Parameters - 获取函数参数类型
type P = Parameters<typeof fn>;  // []

// InstanceType - 获取类实例类型
class MyClass {
  constructor(public x: number, public y: number) {}
}

type MyInstance = InstanceType<typeof MyClass>;  // MyClass
```

### 4.2 自定义工具类型

```typescript
// DeepPartial - 深度可选
type DeepPartial<T> = {
  [P in keyof T]?: T[P] extends object
    ? DeepPartial<T[P]>
    : T[P];
};

interface Config {
  database: {
    host: string;
    port: number;
  };
  server: {
    port: number;
  };
}

const partialConfig: DeepPartial<Config> = {
  database: {
    // host: 'localhost', // 可选
  }
};

// DeepRequired - 深度必需
type DeepRequired<T> = {
  [P in keyof T]-?: T[P] extends object
    ? DeepRequired<T[P]>
    : T[P];
};

// DeepReadonly - 深度只读
type DeepReadonly<T> = {
  readonly [P in keyof T]: T[P] extends object
    ? DeepReadonly<T[P]>
    : T[P];
};

// readonly 嵌套数组/元组特殊处理
type DeepReadonly<T> = {
  readonly [P in keyof T]: T[P] extends (infer U)[]
    ? readonly DeepReadonly<U>[]
    : T[P] extends object
    ? DeepReadonly<T[P]>
    : T[P];
};

// Optional - 指定属性可选
type Optional<T, K extends keyof T> = Omit<T, K> & Partial<Pick<T, K>>;

type UserWithOptionalEmail = Optional<User, 'email'>;
// { id: number; name: string; email?: string; }

// Mutable - 取消只读
type Mutable<T> = {
  -readonly [P in keyof T]: T[P];
};

// Get - 深度获取对象属性类型
type Get<T, P> = P extends `${infer K}.${infer Rest}`
  ? K extends keyof T
    ? Get<T[K], Rest>
    : never
  : P extends keyof T
  ? T[P]
  : never;

interface Nested {
  user: {
    name: string;
    age: number;
  };
}

type UserName = Get<Nested, 'user.name'>;  // string
```

---

## 五、类型推断与类型兼容

### 5.1 类型推断

```typescript
// 基础推断
let x = 3;           // 推断为 number
let y = 'hello';     // 推断为 string
let z = [1, 2, 3];   // 推断为 number[]

// 最佳通用类型
let arr = [0, 1, null]; // 推断为 (number | null)[]

// 上下文类型
window.onclick = function(event) {
  console.log(event.target); // event 推断为 MouseEvent
};

// 函数返回类型推断
function add(a: number, b: number) {
  return a + b; // 推断返回类型为 number
}

// 类型断言触发推断
function create() {
  return [] as string[]; // 明确类型
}
```

### 5.2 类型兼容

```typescript
// 结构化类型（鸭式类型）
interface Pet {
  name: string;
}

class Dog {
  name: string;
  age: number;
}

let pet: Pet;
pet = new Dog(); // ✅ Dog 至少包含 Pet 的属性

// 函数兼容性
let fn1 = (a: number) => {};
let fn2 = (a: number, b: string) => {};

fn1 = fn2; // ✅ 参数少的可以赋值给参数多的
// fn2 = fn1; // ❌ 反之不行

// 返回值兼容性
interface Point1D {
  x: number;
}

interface Point2D {
  x: number;
  y: number;
}

function get1D(): Point1D {
  return { x: 0 };
}

function get2D(): Point2D {
  return { x: 0, y: 0 };
}

let p1: () => Point1D = get2D; // ✅ 返回值多的可以赋值给少的
// let p2: () => Point2D = get1D; // ❌ 反之不行

// 枚举兼容性
enum Status {
  Pending,
  Approved,
  Rejected
}

let status: Status;
status = Status.Pending; // ✅
// status = 0; // ❌ 数字不能赋值给枚举
```

---

## 六、类型声明文件

### 6.1 编写声明文件

```typescript
// 全局变量声明
// globals.d.ts
declare const API_BASE_URL: string;
declare const VERSION: string;

// 全局函数声明
declare function log(message: string): void;

// 全局类声明
declare class Logger {
  constructor(options?: { level: string });
  log(message: string): void;
  error(error: Error): void;
}

// 模块声明
declare module 'my-library' {
  export function init(): void;
  export function configure(options: { apiKey: string }): void;
}

// 模块扩展
declare module 'express' {
  interface Request {
    user?: {
      id: number;
      name: string;
    };
  }
}

// 类型导入/导出
export type { User, Admin } from './types';
export { UserService } from './services';
```

### 6.2 使用第三方类型

```typescript
// 安装类型定义
// npm install --save-dev @types/lodash

import _ from 'lodash';

// 自动类型推断
const numbers = [1, 2, 3];
const shuffled = _.shuffle(numbers);

// 手动声明（没有类型包时）
// declarations.d.ts
declare module 'some-untyped-library' {
  interface Options {
    debug?: boolean;
  }

  function init(options?: Options): void;
  export = init;
}
```

---

## 七、类型体操实战

### 7.1 实用类型模式

```typescript
// 1. 函数重载
function process(value: string): string;
function process(value: number): number;
function process(value: string | number): string | number {
  return value;
}

const r1 = process('hello'); // string
const r2 = process(123);     // number

// 2. 递归类型
type JSONValue =
  | string
  | number
  | boolean
  | null
  | JSONValue[]
  | { [key: string]: JSONValue };

const data: JSONValue = {
  name: '张三',
  age: 25,
  tags: ['a', 'b'],
  meta: null
};

// 3. 模板字面量类型
type EventName<T extends string> = `on${Capitalize<T>}`;

type ClickEvent = EventName<'click'>; // 'onClick'

type CssProperty = 'color' | 'background';
type CssValue<T extends CssProperty> =
  T extends 'color' ? string :
  T extends 'background' ? string :
  never;

// 4. 条件类型链
type DeepMutable<T> = {
  -readonly [P in keyof T]: T[P] extends object
    ? DeepMutable<T[P]>
    : T[P];
};

// 5. 映射类型 + 条件类型
type Getters<T> = {
  [P in keyof T as `get${Capitalize<string & P>}`]: () => T[P];
};

interface Person {
  name: string;
  age: number;
}

type PersonGetters = Getters<Person>;
// {
//   getName: () => string;
//   getAge: () => number;
// }
```

### 7.2 高级类型推导

```typescript
// 提取 Promise 的返回类型
type Awaited<T> = T extends Promise<infer U> ? U : T;

async function fetchData() {
  return { id: 1, name: '张三' };
}

type DataType = Awaited<ReturnType<typeof fetchData>>;
// { id: number; name: string; }

// 提取函数参数的类型
type FirstParameter<T extends (...args: any) => any> =
  T extends (first: infer F, ...rest: any) => any ? F : never;

function greet(name: string, age: number): void {}

type GreetFirstParam = FirstParameter<typeof greet>; // string

// 元组转联合
type TupleToUnion<T extends any[]> = T[number];

type Tuple = ['a', 'b', 'c'];
type Union = TupleToUnion<Tuple>; // 'a' | 'b' | 'c'

// 联合转交叉（利用函数参数逆变）
type UnionToIntersection<U> =
  (U extends any ? (k: U) => void : never) extends (k: infer I) => void
    ? I
    : never;

type U = { a: string } | { b: number };
type I = UnionToIntersection<U>; // { a: string; } & { b: number; }

// Last of Tuple
type Last<T extends any[]> = T extends [...any, infer L] ? L : never;

type LastElement = Last<[1, 2, 3]>; // 3
```

---

## 八、常见问题与避坑指南

### ❌ 问题 1：any 滥用

```typescript
// ❌ 任何类型都用 any
function process(data: any) {
  return data.value; // 失去类型检查
}

// ✅ 使用泛型
function process<T extends { value: any }>(data: T): T['value'] {
  return data.value;
}

// ✅ 使用 unknown（更安全）
function processUnknown(data: unknown) {
  if (typeof data === 'object' && data !== null && 'value' in data) {
    return (data as { value: any }).value;
  }
}
```

### ❌ 问题 2：类型断言滥用

```typescript
// ❌ 过度使用断言
const value = response as string;

// ✅ 使用类型守卫
function isString(value: unknown): value is string {
  return typeof value === 'string';
}

if (isString(response)) {
  console.log(response.toUpperCase());
}
```

### ❌ 问题 3：可空属性未检查

```typescript
interface User {
  name: string;
  email?: string;
}

function sendEmail(user: User) {
  // ❌ 可能报错
  // console.log(user.email.toUpperCase());

  // ✅ 检查后再使用
  if (user.email) {
    console.log(user.email.toUpperCase());
  }

  // ✅ 或使用可选链
  console.log(user.email?.toUpperCase());
}
```

---

## 九、总结与记忆要点

### 🎯 核心记忆口诀

```
类型注解显式声明
联合交叉自由组合
类型守卫缩小范围
泛型参数灵活复用
条件类型实现逻辑
工具类型简化代码
类型推导减少注解
类型声明扩展库
类型体操进阶必备
```

### 📚 关键技术点

1. **基础类型**：string、number、boolean、null、undefined、array、object
2. **高级类型**：联合、交叉、字面量、类型守卫、判别联合
3. **泛型**：泛型函数、泛型接口、泛型类、泛型约束
4. **工具类型**：Partial、Required、Readonly、Pick、Omit、Record
5. **类型推导**：typeof、keyof、infer、条件类型
6. **声明文件**：.d.ts、declare、module augmentation
7. **类型体操**：映射类型、递归类型、模板字面量类型

---

## 十、实战练习建议

1. 重构一个 JavaScript 项目为 TypeScript
2. 为没有类型的第三方库编写声明文件
3. 实现一个完整的类型守卫工具集
4. 编写工具类型处理表单数据转换
5. 用泛型实现类型安全的 API 客户端

---

**下一步学习**：👉 [泛型与高级类型](./泛型与高级类型.md)
