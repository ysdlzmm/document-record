# 构建工具

## 一、构建工具发展史

### 1.1 演进历程

```
无构建工具 → Grunt/Gulp → Webpack → Vite/Rollup/esbuild
  (手动)    (任务流)   (模块打包)   (新一代构建工具)
```

| 工具 | 特点 | 优势 | 劣势 |
|------|------|------|------|
| **Grunt/Gulp** | 任务流 | 配置简单 | 缺乏模块化 |
| **Webpack** | 模块打包 | 生态丰富 | 配置复杂、构建慢 |
| **Rollup** | ES Module 打包 | Tree-shaking 好 | 不支持 HMR |
| **Vite** | 开发服务器 | 极速启动 | 生产环境用 Rollup |
| **esbuild** | Go 编写 | 构建速度极快 | 插件生态较少 |

---

## 二、Vite（推荐）

### 2.1 为什么选择 Vite？

**核心优势**
- **极速启动**：利用浏览器原生 ES Module，无需打包
- **即时热更新**：HMR 速度不随项目规模增大而变慢
- **开箱即用**：TypeScript、CSS 预处理器、JSX 等无需配置
- **优化生产构建**：使用 Rollup 进行代码分割和优化

### 2.2 项目创建

```bash
# 创建 Vite 项目
npm create vite@latest my-app -- --template react-ts

# 或使用模板
npm create vite@latest my-app --template vanilla
npm create vite@latest my-app --template vue-ts
npm create vite@latest my-app --template react-swc

# 进入项目并安装依赖
cd my-app
npm install

# 启动开发服务器
npm run dev
```

### 2.3 配置文件

```javascript
// vite.config.js / vite.config.ts
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';
import path from 'path';

export default defineConfig({
  // 插件
  plugins: [
    react({
      // 使用 SWC 替代 Babel（更快）
      babel: {
        plugins: ['@babel/plugin-proposal-decorators']
      }
    })
  ],

  // 路径别名
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './src'),
      '@components': path.resolve(__dirname, './src/components'),
      '@utils': path.resolve(__dirname, './src/utils')
    }
  },

  // 开发服务器配置
  server: {
    port: 3000,
    open: true, // 自动打开浏览器
    proxy: {
      '/api': {
        target: 'http://localhost:8080',
        changeOrigin: true,
        rewrite: (path) => path.replace(/^\/api/, '')
      }
    }
  },

  // 构建配置
  build: {
    outDir: 'dist',
    assetsDir: 'assets',
    sourcemap: false, // 生产环境不生成 sourcemap
    minify: 'terser', // 或 'esbuild'
    rollupOptions: {
      output: {
        // 手动分包
        manualChunks: {
          'react-vendor': ['react', 'react-dom'],
          'ui-library': ['antd', '@ant-design/icons']
        },
        // 文件命名
        chunkFileNames: 'js/[name]-[hash].js',
        entryFileNames: 'js/[name]-[hash].js',
        assetFileNames: '[ext]/[name]-[hash].[ext]'
      }
    },
    // Chunk 大小警告限制
    chunkSizeWarningLimit: 1000
  },

  // CSS 配置
  css: {
    modules: {
      localsConvention: 'camelCase' // 驼峰化类名
    },
    preprocessorOptions: {
      scss: {
        additionalData: `@import "@/styles/variables.scss";`
      }
    }
  },

  // 环境变量
  define: {
    __APP_VERSION__: JSON.stringify('1.0.0')
  },

  // 依赖优化
  optimizeDeps: {
    include: ['react', 'react-dom'],
    exclude: ['your-custom-dep']
  }
});
```

### 2.4 环境变量

```bash
# .env                # 所有环境
VITE_APP_TITLE=My App
VITE_API_BASE_URL=https://api.example.com

# .env.development    # 开发环境
VITE_API_BASE_URL=http://localhost:8080

# .env.production     # 生产环境
VITE_API_BASE_URL=https://api.example.com
```

```javascript
// 代码中使用
const apiBaseUrl = import.meta.env.VITE_API_BASE_URL;
const appTitle = import.meta.env.VITE_APP_TITLE;

// TypeScript 类型支持
/// <reference types="vite/client" />

interface ImportMetaEnv {
  readonly VITE_APP_TITLE: string;
  readonly VITE_API_BASE_URL: string;
}

interface ImportMeta {
  readonly env: ImportMetaEnv;
}
```

### 2.5 常用插件

```javascript
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';
import legacy from '@vitejs/plugin-legacy'; // 支持旧浏览器
import svgr from 'vite-plugin-svgr'; // SVG 组件化
import vitePluginImp from 'vite-plugin-imp'; // 按需引入
import { visualizer } from 'rollup-plugin-visualizer'; // 包分析

export default defineConfig({
  plugins: [
    react(),

    // SVG 转 React 组件
    svgr({
      svgrOptions: {
        icon: true
      }
    }),

    // 按需引入（如 Ant Design）
    vitePluginImp({
      libList: [
        {
          libName: 'antd',
          style: (name) => `antd/es/${name}/style`
        }
      ]
    }),

    // 兼容旧浏览器
    legacy({
      targets: ['defaults', 'not IE 11']
    }),

    // 打包分析
    visualizer({
      open: true,
      gzipSize: true,
      brotliSize: true
    })
  ]
});
```

---

## 三、Webpack

### 3.1 核心概念

```javascript
// webpack.config.js
const path = require('path');

module.exports = {
  // 入口
  entry: './src/index.js',

  // 输出
  output: {
    path: path.resolve(__dirname, 'dist'),
    filename: 'js/[name].[contenthash].js',
    clean: true, // 清理输出目录
    publicPath: '/'
  },

  // 模式
  mode: 'production', // 或 'development'

  // Loader（转换代码）
  module: {
    rules: [
      // JavaScript/JSX
      {
        test: /\.(js|jsx)$/,
        exclude: /node_modules/,
        use: {
          loader: 'babel-loader',
          options: {
            presets: ['@babel/preset-env', '@babel/preset-react'],
            plugins: ['@babel/plugin-proposal-decorators']
          }
        }
      },

      // TypeScript
      {
        test: /\.(ts|tsx)$/,
        exclude: /node_modules/,
        use: 'ts-loader'
      },

      // CSS
      {
        test: /\.css$/,
        use: ['style-loader', 'css-loader', 'postcss-loader']
      },

      // SCSS
      {
        test: /\.scss$/,
        use: [
          'style-loader',
          'css-loader',
          'postcss-loader',
          {
            loader: 'sass-loader',
            options: {
              additionalData: `@import "@/styles/variables.scss";`
            }
          }
        ]
      },

      // 图片
      {
        test: /\.(png|jpe?g|gif|svg)$/,
        type: 'asset',
        parser: {
          dataUrlCondition: {
            maxSize: 10 * 1024 // 小于 10KB 转 base64
          }
        },
        generator: {
          filename: 'images/[name].[hash:6][ext]'
        }
      },

      // 字体
      {
        test: /\.(woff2?|eot|ttf|otf)$/,
        type: 'asset/resource',
        generator: {
          filename: 'fonts/[name].[hash:6][ext]'
        }
      }
    ]
  },

  // 插件
  plugins: [
    // HTML 模板
    new HtmlWebpackPlugin({
      template: './public/index.html',
      filename: 'index.html',
      minify: {
        removeComments: true,
        collapseWhitespace: true
      }
    }),

    // 环境变量
    new webpack.DefinePlugin({
      'process.env': JSON.stringify(process.env)
    }),

    // CSS 提取
    new MiniCssExtractPlugin({
      filename: 'css/[name].[contenthash].css',
      chunkFilename: 'css/[id].[contenthash].css'
    }),

    // 清理目录
    new CleanWebpackPlugin(),

    // 复制静态资源
    new CopyPlugin({
      patterns: [
        { from: 'public', to: '' }
      ]
    })
  ],

  // 解析
  resolve: {
    extensions: ['.js', '.jsx', '.ts', '.tsx', '.json'],
    alias: {
      '@': path.resolve(__dirname, 'src'),
      '@components': path.resolve(__dirname, 'src/components')
    }
  },

  // 开发服务器
  devServer: {
    static: {
      directory: path.join(__dirname, 'public')
    },
    port: 3000,
    open: true,
    hot: true,
    compress: true,
    proxy: {
      '/api': {
        target: 'http://localhost:8080',
        changeOrigin: true,
        pathRewrite: { '^/api': '' }
      }
    }
  },

  // 优化
  optimization: {
    minimize: true,
    minimizer: [
      new TerserPlugin({
        terserOptions: {
          compress: {
            drop_console: true // 删除 console
          }
        }
      }),
      new CssMinimizerPlugin()
    ],

    // 代码分割
    splitChunks: {
      chunks: 'all',
      cacheGroups: {
        // 第三方库
        vendor: {
          test: /[\\/]node_modules[\\/]/,
          name: 'vendors',
          priority: 10
        },
        // React 相关
        react: {
          test: /[\\/]node_modules[\\/](react|react-dom)[\\/]/,
          name: 'react',
          priority: 20
        },
        // 公共代码
        common: {
          minChunks: 2,
          priority: 5,
          reuseExistingChunk: true
        }
      }
    },

    // 运行时代码单独提取
    runtimeChunk: {
      name: 'runtime'
    }
  },

  // Source Map
  devtool: 'eval-cheap-module-source-map', // 开发环境
  // devtool: 'hidden-source-map', // 生产环境
};
```

### 3.2 性能优化策略

```javascript
module.exports = {
  optimization: {
    // Tree Shaking
    usedExports: true,
    sideEffects: false,

    // 代码压缩
    minimize: true,

    // 模块串联（减少函数调用）
    concatenateModules: true,

    // 作用域提升
    // Scope Hoisting 自动开启

    splitChunks: {
      chunks: 'all',
      maxInitialRequests: 10, // 最大并行请求数
      maxAsyncRequests: 10,   // 最大异步请求数
      cacheGroups: {
        vendors: {
          test: /[\\/]node_modules[\\/]/,
          priority: -10,
          name(module) {
            const packageName = module.context.match(/[\\/]node_modules[\\/](.*?)([\\/]|$)/)[1];
            return `vendor.${packageName.replace('@', '')}`;
          }
        }
      }
    }
  },

  // 持久化缓存
  cache: {
    type: 'filesystem', // 文件系统缓存
    buildDependencies: {
      config: [__filename] // 配置文件变化时重新构建
    }
  },

  // 并行处理
  parallelism: os.cpus().length - 1
};
```

---

## 四、Rollup

### 4.1 配置示例

```javascript
// rollup.config.js
import resolve from '@rollup/plugin-node-resolve';
import commonjs from '@rollup/plugin-commonjs';
import typescript from '@rollup/plugin-typescript';
import { terser } from 'rollup-plugin-terser';
import peerDepsExternal from 'rollup-plugin-peer-deps-external';

export default {
  // 入口
  input: 'src/index.ts',

  // 输出
  output: [
    {
      file: 'dist/index.js',
      format: 'cjs', // CommonJS
      exports: 'named'
    },
    {
      file: 'dist/index.esm.js',
      format: 'esm', // ES Module
      exports: 'named'
    },
    {
      file: 'dist/index.umd.js',
      format: 'umd', // UMD
      name: 'MyLibrary',
      globals: {
        react: 'React'
      }
    }
  ],

  // 插件
  plugins: [
    peerDepsExternal(), // 外部化 peerDependencies
    resolve(), // 解析 node_modules
    commonjs(), // 转换 CommonJS 为 ES6
    typescript(), // TypeScript 支持
    terser() // 压缩代码
  ],

  // 外部依赖（不打包）
  external: ['react', 'react-dom']
};
```

### 4.2 组件库打包

```javascript
import { babel } from '@rollup/plugin-babel';
import postcss from 'rollup-plugin-postcss';
import svg from 'rollup-plugin-svg';

export default {
  input: 'src/index.ts',
  output: [
    {
      file: 'dist/index.cjs.js',
      format: 'cjs'
    },
    {
      file: 'dist/index.esm.js',
      format: 'esm'
    }
  ],
  plugins: [
    svg(),
    postcss({
      modules: true,
      inject: false // 提取 CSS 到单独文件
    }),
    babel({
      babelHelpers: 'bundled',
      exclude: 'node_modules/**',
      extensions: ['.js', '.jsx', '.ts', '.tsx']
    })
  ]
};
```

---

## 五、esbuild

### 5.1 基础使用

```javascript
const esbuild = require('esbuild');

// 异步构建
esbuild.build({
  entryPoints: ['src/index.ts'],
  bundle: true,
  outfile: 'dist/bundle.js',
  minify: true,
  sourcemap: true,
  target: 'es2020'
}).catch(() => process.exit(1));

// 同步构建
esbuild.buildSync({
  entryPoints: ['src/index.ts'],
  bundle: true,
  outfile: 'dist/bundle.js'
});

// 开发服务器
esbuild.context({
  entryPoints: ['src/index.ts'],
  bundle: true,
  outfile: 'dist/bundle.js',
  sourcemap: true,
  loader: {
    '.svg': 'dataurl',
    '.png': 'dataurl'
  }
}).then(ctx => {
  ctx.watch(); // 监听文件变化
  ctx.serve({
    servedir: 'public'
  });
});
```

### 5.2 性能对比

```
Webpack:  10s - 30s
Rollup:   5s - 15s
esbuild:  0.1s - 1s (100倍速度提升)
```

---

## 六、构建工具选择指南

### 6.1 场景对比

| 场景 | 推荐工具 | 原因 |
|------|----------|------|
| **新项目开发** | Vite | 开发体验好、启动快、配置简单 |
| **组件库开发** | Rollup | Tree-shaking 好、输出格式多 |
| **大型应用** | Webpack | 生态丰富、插件多、成熟稳定 |
| **极简项目** | esbuild | 构建速度极快 |
| **微前端** | Webpack/Module Federation | 模块联邦支持好 |

### 6.2 迁移建议

```bash
# Webpack → Vite
1. 安装 Vite
2. 配置 vite.config.js
3. 修改 index.html（添加入口 script）
4. 调整环境变量（VITE_ 前缀）
5. 处理动态导入（vite 的限制）

# create-react-app → Vite
npm create vite@latest my-app --template react
# 复制 src 文件到新项目
# 安装必要依赖
```

---

## 七、构建优化技巧

### 7.1 代码分割策略

```javascript
// 路由级分割
const Home = lazy(() => import('./routes/Home'));
const About = lazy(() => import('./routes/About'));

// 组件级分割
const HeavyChart = lazy(() => import('./components/HeavyChart'));

// 条件分割
const AdminPanel = lazy(() =>
  import('./components/AdminPanel')
);

// 动态导入函数
async function loadModule() {
  const { default: module } = await import('./utils/heavy');
  return module;
}
```

### 7.2 Tree Shaking

```javascript
// ✅ 使用 ES Module
export function add(a, b) {
  return a + b;
}

export function subtract(a, b) {
  return a - b;
}

// 使用时只导入需要的
import { add } from './math';

// ❌ CommonJS 无法 Tree Shaking
module.exports.add = function(a, b) {
  return a + b;
};

// package.json 设置 sideEffects
{
  "sideEffects": false, // 整个包可 Tree Shaking
  "sideEffects": ["*.css"] // 仅 CSS 文件有副作用
}
```

### 7.3 压缩与混淆

```javascript
// Terser 配置
new TerserPlugin({
  terserOptions: {
    compress: {
      drop_console: true,
      drop_debugger: true,
      pure_funcs: ['console.log'] // 删除特定函数
    },
    mangle: {
      safari10: true // 兼容 Safari 10
    }
  },
  extractComments: false // 不提取注释
});

// CSS 压缩
new CssMinimizerPlugin({
  minimizerOptions: {
    preset: [
      'default',
      {
        discardComments: { removeAll: true }
      }
    ]
  }
});
```

---

## 八、常见问题与避坑指南

### ❌ 问题 1：Vite 生产环境报错

```javascript
// ❌ 问题：使用 require
const data = require('./data.json');

// ✅ 解决：使用 import
import data from './data.json';
```

### ❌ 问题 2：环境变量不生效

```bash
# ❌ 错误命名
API_BASE_URL=https://api.example.com

# ✅ Vite 必须加 VITE_ 前缀
VITE_API_BASE_URL=https://api.example.com
```

### ❌ 问题 3：构建后路径错误

```javascript
// vite.config.js
export default defineConfig({
  // ❌ 默认是绝对路径
  base: '/',

  // ✅ 子目录部署
  base: '/subdirectory/',

  // ✅ CDN 部署
  base: 'https://cdn.example.com/'
});
```

---

## 九、总结与记忆要点

### 🎯 核心记忆口诀

```
Vite 极速开发体验
Webpack 生态最强
Rollup 库打包首选
esbuild 速度最快
代码分割按需加载
Tree Shaking 减小体积
Source Map 方便调试
环境变量管理配置
```

### 📚 关键技术点

1. **Vite**：ES Module、极速 HMR、Rollup 打包
2. **Webpack**：Loader 转换、Plugin 扩展、代码分割
3. **Rollup**：Tree-shaking、多格式输出、组件库
4. **优化**：代码分割、Tree Shaking、压缩混淆
5. **选择**：开发用 Vite、库用 Rollup、大型项目用 Webpack

---

## 十、实战练习建议

1. 从零搭建一个 Vite + React + TypeScript 项目
2. 配置 Vite 的路径别名、代理、环境变量
3. 使用 Webpack 配置一个多入口项目
4. 用 Rollup 打包一个可发布的组件库
5. 对比不同构建工具的构建速度和产物大小

---

**下一步学习**：👉 [包管理器](./包管理器.md)
